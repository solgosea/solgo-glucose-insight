import '../status_level.dart';

class StatusRuleThresholdBand {
  final StatusLevel level;
  final String label;

  const StatusRuleThresholdBand({
    required this.level,
    required this.label,
  });
}
