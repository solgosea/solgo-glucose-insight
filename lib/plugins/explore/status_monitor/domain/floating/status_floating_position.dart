class StatusFloatingPosition {
  final int x;
  final int y;

  const StatusFloatingPosition({
    required this.x,
    required this.y,
  });

  static const defaultPosition = StatusFloatingPosition(x: 24, y: 160);
}
