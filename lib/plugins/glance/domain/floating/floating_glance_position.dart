class FloatingGlancePosition {
  final double x;
  final double y;

  const FloatingGlancePosition({
    required this.x,
    required this.y,
  });

  static const defaultPosition = FloatingGlancePosition(x: 24, y: 120);
}
